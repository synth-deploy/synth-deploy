{
  "name": "synth-deploy",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/core && npm run build -w packages/command -w packages/envoy -w packages/ui",
    "dev": "npm run dev --workspace=packages/command",
    "dev:ui": "npm run dev --workspace=packages/ui",
    "build:ui": "npm run build --workspace=packages/ui",
    "test": "npm test --workspace=packages/core --workspace=packages/command --workspace=packages/envoy --workspace=packages/ui",
    "test:coverage": "npm test --workspace=packages/core --workspace=packages/command --workspace=packages/envoy --workspace=packages/ui -- --coverage",
    "test:integration": "vitest run --config tests/vitest.config.ts",
    "test:scenarios": "vitest run --config tests/vitest.config.ts scenarios/",
    "clean": "rm -rf packages/*/dist packages/*/*.tsbuildinfo"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.18"
  }
}
