{
  "name": "deploystack",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspace=packages/server",
    "clean": "rm -rf packages/*/dist"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
