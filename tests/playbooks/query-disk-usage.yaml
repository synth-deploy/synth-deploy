name: "Query: disk usage on staging"
type: query
tags: [query, staging]

setup:
  environments:
    - name: staging
      variables:
        APP_ENV: staging

operation:
  type: query
  intent: "Show disk usage on staging servers"
  environmentRef: staging

assertions:
  - responseStatus: 201
  - statusIn: [pending, planning, awaiting_approval]
