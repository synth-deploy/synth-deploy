---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Synth — Deployment Intelligence, Embedded">
  <!-- Hero -->
  <section class="relative overflow-hidden">
    <div class="absolute inset-0 pointer-events-none">
      <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-ds-accent/5 rounded-full blur-[120px]"></div>
      <div class="absolute bottom-1/3 right-1/4 w-80 h-80 bg-ds-partition/5 rounded-full blur-[100px]"></div>
    </div>
    <div class="relative mx-auto max-w-6xl px-6 pt-24 pb-20 text-center">
      <div class="animate-fade-in-up">
        <h1 class="text-5xl md:text-6xl font-display font-bold leading-tight mb-6">
          Stop writing runbooks.<br />
          <span class="text-ds-accent">Start deploying intelligently.</span>
        </h1>
        <p class="text-xl text-ds-text-secondary max-w-2xl mx-auto mb-10 leading-relaxed">
          You say what and where. Synth figures out the how — then shows you
          every step and every decision before it touches a thing.
        </p>
        <div class="flex items-center justify-center gap-4">
          <a
            href="https://github.com/jmfullerton96/synth-deploy"
            target="_blank"
            rel="noopener"
            class="rounded-md bg-ds-accent px-6 py-3 text-ds-bg font-semibold hover:bg-ds-accent/90 transition-colors"
          >
            Get Started
          </a>
          <a
            href="/docs"
            class="rounded-md border border-ds-border px-6 py-3 text-ds-text hover:border-ds-accent-border hover:bg-ds-surface transition-colors"
          >
            Read the Docs
          </a>
          <a href="/roadmap" class="rounded-md border border-ds-border px-6 py-3 text-ds-text-secondary hover:border-ds-accent-border hover:bg-ds-surface transition-colors">
            Roadmap
          </a>
        </div>
      </div>

      <!-- Terminal preview -->
      <div class="mt-16 animate-fade-in-up animate-delay-2">
        <div class="glass-card accent-glow max-w-3xl mx-auto overflow-hidden">
          <div class="flex items-center gap-2 px-4 py-3 border-b border-ds-border">
            <div class="w-3 h-3 rounded-full bg-ds-failed/60"></div>
            <div class="w-3 h-3 rounded-full bg-ds-warning/60"></div>
            <div class="w-3 h-3 rounded-full bg-ds-succeeded/60"></div>
            <span class="ml-2 text-xs text-ds-text-muted font-mono">synth</span>
          </div>
          <div class="p-6 font-mono text-sm leading-relaxed text-left">
            <p class="text-ds-text-muted">$ synth deploy web-app v2.4.1 --partition acme-corp</p>
            <p class="mt-3 text-ds-accent">Agent: Planned 3-step pipeline for web-app v2.4.1</p>
            <p class="text-ds-text-secondary">  1. Install dependencies <span class="text-ds-succeeded">&#10003;</span> 14.2s</p>
            <p class="text-ds-text-secondary">  2. Run migrations <span class="text-ds-succeeded">&#10003;</span> 3.1s</p>
            <p class="text-ds-text-secondary">  3. Health check <span class="text-ds-succeeded">&#10003;</span> 0.2s</p>
            <p class="mt-3 text-ds-accent">Decision: Resolved 4 variables (1 conflict: APP_ENV — environment wins)</p>
            <p class="text-ds-succeeded mt-1">Deployment succeeded in 17.5s</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Section 1: Simple Setup -->
  <section id="features" class="py-28 border-t border-ds-border">
    <div class="mx-auto max-w-6xl px-6">
      <div class="text-center mb-16 scroll-reveal">
        <p class="section-heading">Simple to Set Up</p>
        <h2 class="text-3xl md:text-4xl font-display font-bold mb-4">Two components. One command.</h2>
        <p class="text-ds-text-secondary text-lg max-w-2xl mx-auto">
          Synth runs as two lightweight services — the Synth server and Envoy — that communicate
          over HTTP. No complex infrastructure. No vendor lock-in. Self-hosted by design.
        </p>
      </div>

      <div class="grid md:grid-cols-2 gap-8 max-w-4xl mx-auto">
        <!-- Synth server card -->
        <div class="glass-card p-8 scroll-reveal scroll-reveal-delay-1">
          <div class="flex items-center gap-3 mb-4">
            <div class="entity-badge border-ds-server/30 bg-ds-server/10 text-ds-server">
              Synth
            </div>
          </div>
          <h3 class="text-lg font-semibold mb-2">The brain</h3>
          <p class="text-ds-text-secondary leading-relaxed text-sm">
            Central server that manages operations, partitions, and environments.
            Runs the intelligent agent, exposes the REST API, and hosts the MCP server.
          </p>
          <div class="mt-4 font-mono text-xs text-ds-text-muted bg-ds-bg/50 rounded-md px-4 py-3">
            docker run synth-deploy/server
          </div>
        </div>

        <!-- Envoy card -->
        <div class="glass-card p-8 scroll-reveal scroll-reveal-delay-2">
          <div class="flex items-center gap-3 mb-4">
            <div class="entity-badge border-ds-envoy/30 bg-ds-envoy/10 text-ds-envoy">
              Envoy
            </div>
          </div>
          <h3 class="text-lg font-semibold mb-2">The hands</h3>
          <p class="text-ds-text-secondary leading-relaxed text-sm">
            Lightweight agents deployed on target machines. Execute deployment steps,
            run health checks, and report results back to Synth.
          </p>
          <div class="mt-4 font-mono text-xs text-ds-text-muted bg-ds-bg/50 rounded-md px-4 py-3">
            docker run synth-deploy/envoy
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Section 2: Intelligent Decisions -->
  <section class="py-28 border-t border-ds-border relative overflow-hidden">
    <div class="absolute inset-0 pointer-events-none">
      <div class="absolute top-1/2 left-0 w-96 h-96 bg-ds-dt-plan/5 rounded-full blur-[120px]"></div>
      <div class="absolute bottom-0 right-1/4 w-72 h-72 bg-ds-debrief/5 rounded-full blur-[100px]"></div>
    </div>

    <div class="relative mx-auto max-w-6xl px-6">
      <div class="text-center mb-16 scroll-reveal">
        <p class="section-heading">Intelligent Decisions</p>
        <h2 class="text-3xl md:text-4xl font-display font-bold mb-4">Every action explained. Every decision logged.</h2>
        <p class="text-ds-text-secondary text-lg max-w-2xl mx-auto">
          The agent resolves variables, plans pipelines, handles conflicts, and diagnoses
          failures — then explains all of it in plain language. When something goes wrong
          at 2am, the Debrief tells you exactly what happened.
        </p>
      </div>

      <!-- Decision type showcase -->
      <div class="grid md:grid-cols-3 gap-6 mb-16">
        <div class="glass-card p-6 scroll-reveal scroll-reveal-delay-1">
          <div class="flex items-center gap-2 mb-3">
            <span class="dt-dot bg-ds-dt-plan"></span>
            <span class="text-xs font-semibold uppercase tracking-wide text-ds-dt-plan">Pipeline Plan</span>
          </div>
          <p class="text-sm text-ds-text-secondary leading-relaxed">
            Agent analyzed web-app v2.4.1 and planned a 3-step pipeline:
            install, migrate, verify. Skipped cache-warmup — no Redis configured.
          </p>
        </div>

        <div class="glass-card p-6 scroll-reveal scroll-reveal-delay-2">
          <div class="flex items-center gap-2 mb-3">
            <span class="dt-dot bg-ds-dt-conflict"></span>
            <span class="text-xs font-semibold uppercase tracking-wide text-ds-dt-conflict">Variable Conflict</span>
          </div>
          <p class="text-sm text-ds-text-secondary leading-relaxed">
            APP_ENV defined in both partition (production) and environment (staging).
            Resolved using environment-wins policy. Logged both values for audit.
          </p>
        </div>

        <div class="glass-card p-6 scroll-reveal scroll-reveal-delay-3">
          <div class="flex items-center gap-2 mb-3">
            <span class="dt-dot bg-ds-dt-diagnostic"></span>
            <span class="text-xs font-semibold uppercase tracking-wide text-ds-dt-diagnostic">Diagnostic</span>
          </div>
          <p class="text-sm text-ds-text-secondary leading-relaxed">
            Health check failed on attempt 1/3 (ECONNREFUSED on port 9410).
            Root cause: migration step increased startup time. Retried after 5s — passed.
          </p>
        </div>
      </div>

      <!-- Debrief timeline mockup -->
      <div class="max-w-2xl mx-auto scroll-reveal scroll-reveal-delay-4">
        <div class="glass-card accent-glow overflow-hidden">
          <div class="flex items-center gap-2 px-5 py-3 border-b border-ds-border">
            <div class="entity-badge border-ds-debrief/30 bg-ds-debrief/10 text-ds-debrief text-[10px] py-0.5">
              Debrief
            </div>
            <span class="text-xs text-ds-text-muted ml-auto">web-app v2.4.1 &rarr; acme-corp / production</span>
          </div>
          <div class="p-5 space-y-0">
            <!-- Timeline entries -->
            <div class="flex gap-3 pb-4 relative">
              <div class="flex flex-col items-center">
                <span class="dt-dot bg-ds-dt-plan mt-1"></span>
                <div class="w-px flex-1 bg-ds-border mt-1"></div>
              </div>
              <div>
                <p class="text-xs font-semibold text-ds-dt-plan uppercase tracking-wide">Pipeline Plan</p>
                <p class="text-sm text-ds-text-secondary mt-0.5">Planned 3-step pipeline for web-app v2.4.1</p>
                <p class="text-xs text-ds-text-muted mt-1">Steps: install-deps &rarr; run-migrations &rarr; health-check</p>
              </div>
            </div>

            <div class="flex gap-3 pb-4 relative">
              <div class="flex flex-col items-center">
                <span class="dt-dot bg-ds-dt-config mt-1"></span>
                <div class="w-px flex-1 bg-ds-border mt-1"></div>
              </div>
              <div>
                <p class="text-xs font-semibold text-ds-dt-config uppercase tracking-wide">Configuration</p>
                <p class="text-sm text-ds-text-secondary mt-0.5">Resolved 4 variables across partition and environment</p>
                <p class="text-xs text-ds-text-muted mt-1">1 conflict: APP_ENV — environment value wins (staging)</p>
              </div>
            </div>

            <div class="flex gap-3 pb-4 relative">
              <div class="flex flex-col items-center">
                <span class="dt-dot bg-ds-dt-execution mt-1"></span>
                <div class="w-px flex-1 bg-ds-border mt-1"></div>
              </div>
              <div>
                <p class="text-xs font-semibold text-ds-dt-execution uppercase tracking-wide">Execution</p>
                <p class="text-sm text-ds-text-secondary mt-0.5">All 3 steps completed successfully in 17.5s</p>
                <p class="text-xs text-ds-text-muted mt-1">install-deps: 14.2s | migrations: 3.1s | health: 0.2s</p>
              </div>
            </div>

            <div class="flex gap-3 pb-4 relative">
              <div class="flex flex-col items-center">
                <span class="dt-dot bg-ds-dt-verification mt-1"></span>
                <div class="w-px flex-1 bg-ds-border mt-1"></div>
              </div>
              <div>
                <p class="text-xs font-semibold text-ds-dt-verification uppercase tracking-wide">Verification</p>
                <p class="text-sm text-ds-text-secondary mt-0.5">Health check passed — HTTP 200 on /health in 42ms</p>
                <p class="text-xs text-ds-text-muted mt-1">Endpoint: http://acme-web:3000/health</p>
              </div>
            </div>

            <div class="flex gap-3 relative">
              <div class="flex flex-col items-center">
                <span class="dt-dot bg-ds-dt-completion mt-1"></span>
              </div>
              <div>
                <p class="text-xs font-semibold text-ds-dt-completion uppercase tracking-wide">Completed</p>
                <p class="text-sm text-ds-text-secondary mt-0.5">Deployment succeeded — all checks passed, artifacts written</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Section 3: Core Capabilities -->
  <section class="py-28 border-t border-ds-border">
    <div class="mx-auto max-w-6xl px-6">
      <div class="text-center mb-16 scroll-reveal">
        <p class="section-heading">Capabilities</p>
        <h2 class="text-3xl md:text-4xl font-display font-bold">Intelligence where friction used to be</h2>
      </div>

      <div class="grid md:grid-cols-3 gap-6">
        <div class="glass-card p-8 scroll-reveal scroll-reveal-delay-1">
          <div class="flex items-center gap-2 mb-4">
            <div class="entity-badge border-ds-partition/30 bg-ds-partition/10 text-ds-partition text-[10px] py-0.5">
              Partition
            </div>
          </div>
          <h3 class="text-lg font-semibold mb-2">Absolute Isolation</h3>
          <p class="text-ds-text-secondary leading-relaxed text-sm">
            Each partition is a self-contained deployment context with its own
            variables, history, and configuration. No cross-partition data access
            or failure propagation — ever.
          </p>
        </div>

        <div class="glass-card p-8 scroll-reveal scroll-reveal-delay-2">
          <div class="flex items-center gap-2 mb-4">
            <div class="entity-badge border-ds-debrief/30 bg-ds-debrief/10 text-ds-debrief text-[10px] py-0.5">
              Debrief
            </div>
          </div>
          <h3 class="text-lg font-semibold mb-2">Decision Transparency</h3>
          <p class="text-ds-text-secondary leading-relaxed text-sm">
            Every agent decision is logged in plain language. When something goes wrong,
            the decision log tells you exactly what happened, why, and what to do next.
            Postmortems generate automatically.
          </p>
        </div>

        <div class="glass-card p-8 scroll-reveal scroll-reveal-delay-3">
          <div class="flex items-center gap-2 mb-4">
            <div class="entity-badge border-ds-order/30 bg-ds-order/10 text-ds-order text-[10px] py-0.5">
              Deployment
            </div>
          </div>
          <h3 class="text-lg font-semibold mb-2">Immutable Snapshots</h3>
          <p class="text-ds-text-secondary leading-relaxed text-sm">
            Deployments capture the exact configuration at execution time — steps, variables,
            deploy config. Re-deploy any previous snapshot with confidence. Full audit trail
            from intent to outcome.
          </p>
        </div>

        <div class="glass-card p-8 scroll-reveal scroll-reveal-delay-4">
          <div class="flex items-center gap-2 mb-4">
            <div class="entity-badge border-ds-server/30 bg-ds-server/10 text-ds-server text-[10px] py-0.5">
              MCP
            </div>
          </div>
          <h3 class="text-lg font-semibold mb-2">Protocol-Native</h3>
          <p class="text-ds-text-secondary leading-relaxed text-sm">
            Built on Model Context Protocol from the foundation. Connect external
            data sources, monitoring tools, and CI/CD systems through standardized
            MCP servers — not custom integrations.
          </p>
        </div>

        <div class="glass-card p-8 scroll-reveal scroll-reveal-delay-1">
          <div class="flex items-center gap-2 mb-4">
            <div class="entity-badge border-ds-dt-plan/30 bg-ds-dt-plan/10 text-ds-dt-plan text-[10px] py-0.5">LLM</div>
          </div>
          <h3 class="text-lg font-semibold mb-2">Any Model, Any Provider</h3>
          <p class="text-ds-text-secondary leading-relaxed text-sm">
            Claude, GPT, Gemini, Grok, DeepSeek, Ollama, or any OpenAI-compatible endpoint.
            Run the intelligence entirely on your infrastructure — no data leaves your network.
          </p>
        </div>

        <div class="glass-card p-8 scroll-reveal scroll-reveal-delay-2">
          <div class="flex items-center gap-2 mb-4">
            <div class="entity-badge border-ds-failed/30 bg-ds-failed/10 text-ds-failed text-[10px] py-0.5">Security</div>
          </div>
          <h3 class="text-lg font-semibold mb-2">Hardened by Default</h3>
          <p class="text-ds-text-secondary leading-relaxed text-sm">
            Prompt injection protection, SSRF validation, secrets masking, path traversal guards,
            and webhook HMAC verification. RBAC with SAML/OIDC/LDAP integration.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Section 4: Architecture Flow -->
  <section id="how-it-works" class="py-28 border-t border-ds-border relative overflow-hidden">
    <div class="absolute inset-0 pointer-events-none">
      <div class="absolute bottom-1/4 left-1/3 w-72 h-72 bg-ds-envoy/3 rounded-full blur-[100px]"></div>
    </div>

    <div class="relative mx-auto max-w-6xl px-6">
      <div class="text-center mb-16 scroll-reveal">
        <p class="section-heading">Architecture</p>
        <h2 class="text-3xl md:text-4xl font-display font-bold mb-4">How it all connects</h2>
        <p class="text-ds-text-secondary text-lg max-w-2xl mx-auto">
          The deployment flow moves from intent to execution to explanation.
          Every step is visible. Every decision is recorded.
        </p>
      </div>

      <!-- Flow diagram — vertical timeline -->
      <div class="glass-card p-8 md:p-10 max-w-3xl mx-auto scroll-reveal scroll-reveal-delay-2">
        <div class="space-y-0">
          <!-- Step 1: Intent -->
          <div class="flex gap-4 pb-6 relative">
            <div class="flex flex-col items-center">
              <span class="entity-badge border-ds-text-muted/30 bg-ds-text-muted/10 text-ds-text-secondary text-[10px] py-0.5 whitespace-nowrap">1</span>
              <div class="w-px flex-1 bg-ds-border mt-2"></div>
            </div>
            <div>
              <p class="text-sm font-semibold text-ds-text">Engineer declares intent</p>
              <p class="text-xs text-ds-text-muted mt-1">What to deploy, where to deploy it, and any partition or environment context.</p>
            </div>
          </div>

          <!-- Step 2: Planning -->
          <div class="flex gap-4 pb-6 relative">
            <div class="flex flex-col items-center">
              <span class="entity-badge border-ds-server/30 bg-ds-server/10 text-ds-server text-[10px] py-0.5 whitespace-nowrap">2</span>
              <div class="w-px flex-1 bg-ds-border mt-2"></div>
            </div>
            <div>
              <div class="flex items-center gap-2 mb-1">
                <p class="text-sm font-semibold text-ds-text">Synth reasons and plans</p>
                <span class="entity-badge border-ds-server/30 bg-ds-server/10 text-ds-server text-[10px] py-0.5">Synth</span>
              </div>
              <p class="text-xs text-ds-text-muted mt-1">The agent analyzes the artifact, resolves variables, probes target systems, and generates a deployment plan — steps are not predefined, they're reasoned from context.</p>
            </div>
          </div>

          <!-- Step 3: Approval -->
          <div class="flex gap-4 pb-6 relative">
            <div class="flex flex-col items-center">
              <span class="entity-badge border-ds-dt-plan/30 bg-ds-dt-plan/10 text-ds-dt-plan text-[10px] py-0.5 whitespace-nowrap">3</span>
              <div class="w-px flex-1 bg-ds-border mt-2"></div>
            </div>
            <div>
              <p class="text-sm font-semibold text-ds-text">Engineer reviews and approves</p>
              <p class="text-xs text-ds-text-muted mt-1">Every planned step is visible before execution. Dry-run observations show what will change. Nothing runs until approved.</p>
            </div>
          </div>

          <!-- Step 4: Execution -->
          <div class="flex gap-4 pb-6 relative">
            <div class="flex flex-col items-center">
              <span class="entity-badge border-ds-envoy/30 bg-ds-envoy/10 text-ds-envoy text-[10px] py-0.5 whitespace-nowrap">4</span>
              <div class="w-px flex-1 bg-ds-border mt-2"></div>
            </div>
            <div>
              <div class="flex items-center gap-2 mb-1">
                <p class="text-sm font-semibold text-ds-text">Envoy executes deterministically</p>
                <span class="entity-badge border-ds-envoy/30 bg-ds-envoy/10 text-ds-envoy text-[10px] py-0.5">Envoy</span>
              </div>
              <p class="text-xs text-ds-text-muted mt-1">The approved plan runs exactly as shown — no re-reasoning, no improvisation. Five handlers cover files, processes, config, containers, and verification.</p>
            </div>
          </div>

          <!-- Step 5: Debrief -->
          <div class="flex gap-4 relative">
            <div class="flex flex-col items-center">
              <span class="entity-badge border-ds-debrief/30 bg-ds-debrief/10 text-ds-debrief text-[10px] py-0.5 whitespace-nowrap">5</span>
            </div>
            <div>
              <div class="flex items-center gap-2 mb-1">
                <p class="text-sm font-semibold text-ds-text">Debrief explains everything</p>
                <span class="entity-badge border-ds-debrief/30 bg-ds-debrief/10 text-ds-debrief text-[10px] py-0.5">Debrief</span>
              </div>
              <p class="text-xs text-ds-text-muted mt-1">Every decision from both Synth and Envoy is logged in plain language — 21 decision types covering planning, conflicts, diagnostics, and outcomes.</p>
            </div>
          </div>
        </div>
      </div>

      <!-- Sample outputs: Artifact Analysis + Plan Approval -->
      <div class="grid md:grid-cols-2 gap-6 mt-12 max-w-5xl mx-auto">

        <!-- Artifact Analysis -->
        <div class="glass-card overflow-hidden scroll-reveal scroll-reveal-delay-3">
          <div class="flex items-center gap-2 px-5 py-3 border-b border-ds-border">
            <div class="entity-badge border-ds-dt-plan/30 bg-ds-dt-plan/10 text-ds-dt-plan text-[10px] py-0.5">
              Artifact Analysis
            </div>
            <span class="text-xs text-ds-text-muted ml-auto">web-app v2.3.0</span>
          </div>
          <div class="p-5 space-y-4 text-sm">
            <p class="text-ds-text-secondary leading-relaxed">
              Node.js web application with Express backend and React frontend.
              Requires PostgreSQL and Redis.
            </p>
            <div>
              <p class="text-[10px] font-semibold uppercase tracking-wide text-ds-text-muted mb-2">Dependencies</p>
              <div class="flex flex-wrap gap-1.5">
                <span class="font-mono text-xs px-2 py-0.5 rounded bg-ds-surface text-ds-text-secondary">postgresql</span>
                <span class="font-mono text-xs px-2 py-0.5 rounded bg-ds-surface text-ds-text-secondary">redis</span>
                <span class="font-mono text-xs px-2 py-0.5 rounded bg-ds-surface text-ds-text-secondary">node:20</span>
              </div>
            </div>
            <div>
              <p class="text-[10px] font-semibold uppercase tracking-wide text-ds-text-muted mb-2">Expected Configuration</p>
              <div class="space-y-1 font-mono text-xs">
                <div class="flex gap-2"><span class="text-ds-accent">DB_HOST</span><span class="text-ds-text-muted">PostgreSQL hostname</span></div>
                <div class="flex gap-2"><span class="text-ds-accent">REDIS_URL</span><span class="text-ds-text-muted">Redis connection string</span></div>
                <div class="flex gap-2"><span class="text-ds-accent">APP_ENV</span><span class="text-ds-text-muted">Runtime environment</span></div>
              </div>
            </div>
            <div class="flex items-center justify-between pt-2 border-t border-ds-border">
              <span class="text-xs text-ds-text-muted">Deployment intent</span>
              <span class="text-xs text-ds-text-secondary">Rolling deploy via health check gating</span>
            </div>
            <div class="flex items-center justify-between">
              <span class="text-xs text-ds-text-muted">Confidence</span>
              <div class="flex items-center gap-2">
                <div class="w-16 h-1.5 rounded-full bg-ds-surface overflow-hidden">
                  <div class="h-full rounded-full bg-ds-succeeded" style="width:92%"></div>
                </div>
                <span class="text-xs text-ds-succeeded font-medium">0.92</span>
              </div>
            </div>
          </div>
        </div>

        <!-- Plan Awaiting Approval -->
        <div class="glass-card overflow-hidden scroll-reveal scroll-reveal-delay-4">
          <div class="flex items-center gap-2 px-5 py-3 border-b border-ds-border">
            <div class="entity-badge border-ds-warning/30 bg-ds-warning/10 text-ds-warning text-[10px] py-0.5">
              Awaiting Approval
            </div>
            <span class="text-xs text-ds-text-muted ml-auto">web-app v2.3.0 &rarr; production</span>
          </div>
          <div class="p-5 space-y-4">
            <!-- Assessment -->
            <div class="rounded-md bg-ds-surface/50 px-4 py-3">
              <p class="text-[10px] font-semibold uppercase tracking-wide text-ds-text-muted mb-1.5">Synth Assessment</p>
              <p class="text-xs text-ds-text-secondary leading-relaxed">
                Standard 5-step deploy. One config change: <span class="font-mono text-ds-accent">API_ENDPOINT</span> updated
                to v2 endpoint — validated in staging for 4h. First deployment to production today.
              </p>
            </div>

            <!-- Steps -->
            <div class="space-y-0">
              <div class="flex items-start gap-2.5 py-2 border-b border-ds-border/50">
                <span class="text-[10px] font-mono text-ds-text-muted mt-0.5 w-4 shrink-0">[1]</span>
                <div class="flex-1 min-w-0">
                  <p class="text-xs text-ds-text-secondary">Stop service</p>
                  <p class="text-[10px] text-ds-text-muted font-mono mt-0.5">systemctl stop web-app</p>
                </div>
                <span class="text-[10px] px-1.5 py-0.5 rounded bg-ds-succeeded/10 text-ds-succeeded shrink-0">low</span>
              </div>
              <div class="flex items-start gap-2.5 py-2 border-b border-ds-border/50">
                <span class="text-[10px] font-mono text-ds-text-muted mt-0.5 w-4 shrink-0">[2]</span>
                <div class="flex-1 min-w-0">
                  <p class="text-xs text-ds-text-secondary">Backup current binaries</p>
                  <p class="text-[10px] text-ds-text-muted font-mono mt-0.5">cp -r /opt/web-app/ /opt/web-app.bak/</p>
                </div>
                <span class="text-[10px] px-1.5 py-0.5 rounded bg-ds-failed/10 text-ds-failed shrink-0">high</span>
              </div>
              <div class="flex items-start gap-2.5 py-2 border-b border-ds-border/50">
                <span class="text-[10px] font-mono text-ds-text-muted mt-0.5 w-4 shrink-0">[3]</span>
                <div class="flex-1 min-w-0">
                  <p class="text-xs text-ds-text-secondary">Deploy new artifact</p>
                  <p class="text-[10px] text-ds-text-muted font-mono mt-0.5">tar -xzf web-app-2.3.0.tar.gz -C /opt/web-app/</p>
                </div>
                <span class="text-[10px] px-1.5 py-0.5 rounded bg-ds-succeeded/10 text-ds-succeeded shrink-0">low</span>
              </div>
              <div class="flex items-start gap-2.5 py-2 border-b border-ds-border/50">
                <span class="text-[10px] font-mono text-ds-text-muted mt-0.5 w-4 shrink-0">[4]</span>
                <div class="flex-1 min-w-0">
                  <p class="text-xs text-ds-text-secondary">Apply environment config <span class="text-ds-accent">(1 variable changed)</span></p>
                  <p class="text-[10px] text-ds-text-muted font-mono mt-0.5">envsubst &lt; config.template &gt; /opt/web-app/.env</p>
                </div>
                <span class="text-[10px] px-1.5 py-0.5 rounded bg-ds-succeeded/10 text-ds-succeeded shrink-0">low</span>
              </div>
              <div class="flex items-start gap-2.5 py-2">
                <span class="text-[10px] font-mono text-ds-text-muted mt-0.5 w-4 shrink-0">[5]</span>
                <div class="flex-1 min-w-0">
                  <p class="text-xs text-ds-text-secondary">Start service and verify health</p>
                  <p class="text-[10px] text-ds-text-muted font-mono mt-0.5">systemctl start web-app &amp;&amp; curl -f localhost:8080/health</p>
                </div>
                <span class="text-[10px] px-1.5 py-0.5 rounded bg-ds-succeeded/10 text-ds-succeeded shrink-0">low</span>
              </div>
            </div>

            <!-- Action buttons -->
            <div class="flex items-center gap-2 pt-2">
              <span class="text-xs font-semibold px-3 py-1.5 rounded-md bg-ds-succeeded/15 text-ds-succeeded">Greenlight</span>
              <span class="text-xs px-3 py-1.5 rounded-md bg-ds-surface text-ds-text-secondary">Refine</span>
              <span class="text-xs px-3 py-1.5 rounded-md bg-ds-surface text-ds-text-muted">Reject</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Section 5: Operational Depth -->
  <section class="py-28 border-t border-ds-border">
    <div class="mx-auto max-w-6xl px-6">
      <div class="text-center mb-16 scroll-reveal">
        <p class="section-heading">Operational Depth</p>
        <h2 class="text-3xl md:text-4xl font-display font-bold mb-4">Built for engineers who get paged</h2>
        <p class="text-ds-text-secondary text-lg max-w-2xl mx-auto">
          Synth is designed for the worst moments — when a deploy fails at 2am
          and you need to understand what happened immediately, not after reading logs
          for twenty minutes.
        </p>
      </div>

      <div class="grid md:grid-cols-3 gap-6">
        <div class="glass-card p-6 scroll-reveal scroll-reveal-delay-1">
          <h3 class="text-base font-semibold mb-2 flex items-center gap-2">
            <span class="dt-dot bg-ds-dt-failure"></span>
            Failure Diagnosis
          </h3>
          <p class="text-sm text-ds-text-secondary leading-relaxed">
            When a step fails, the agent investigates: is it a network issue, a config
            problem, or an application error? The Debrief explains the root cause and
            suggests next steps.
          </p>
        </div>

        <div class="glass-card p-6 scroll-reveal scroll-reveal-delay-2">
          <h3 class="text-base font-semibold mb-2 flex items-center gap-2">
            <span class="dt-dot bg-ds-dt-health"></span>
            Health Awareness
          </h3>
          <p class="text-sm text-ds-text-secondary leading-relaxed">
            Pre-flight and post-deploy health checks. Envoys report readiness state,
            lifecycle phase, and deployment capacity. Synth won't send work to a
            draining or degraded Envoy.
          </p>
        </div>

        <div class="glass-card p-6 scroll-reveal scroll-reveal-delay-3">
          <h3 class="text-base font-semibold mb-2 flex items-center gap-2">
            <span class="dt-dot bg-ds-dt-config"></span>
            Variable Resolution
          </h3>
          <p class="text-sm text-ds-text-secondary leading-relaxed">
            Variables cascade from environment to partition to operation. Conflicts are
            detected and resolved by configurable policy. Every resolution is logged —
            no more mystery overrides.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="py-28 border-t border-ds-border relative overflow-hidden">
    <div class="absolute inset-0 pointer-events-none">
      <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-96 h-96 bg-ds-accent/5 rounded-full blur-[120px]"></div>
    </div>
    <div class="relative mx-auto max-w-6xl px-6 text-center scroll-reveal">
      <h2 class="text-3xl md:text-4xl font-display font-bold mb-4">Ready to deploy smarter?</h2>
      <p class="text-ds-text-secondary text-lg mb-8 max-w-xl mx-auto">
        Synth is open source and self-hosted. Get started in minutes.
      </p>
      <div class="flex items-center justify-center gap-4">
        <a
          href="https://github.com/jmfullerton96/synth-deploy"
          target="_blank"
          rel="noopener"
          class="inline-block rounded-md bg-ds-accent px-8 py-3 text-ds-bg font-semibold hover:bg-ds-accent/90 transition-colors"
        >
          View on GitHub
        </a>
        <a
          href="/docs/installation"
          class="rounded-md border border-ds-border px-6 py-3 text-ds-text hover:border-ds-accent-border hover:bg-ds-surface transition-colors"
        >
          Installation Guide
        </a>
        <a href="/roadmap" class="rounded-md border border-ds-border px-6 py-3 text-ds-text hover:border-ds-accent-border hover:bg-ds-surface transition-colors">
          View Roadmap
        </a>
      </div>
    </div>
  </section>
</Layout>

<!-- Scroll reveal observer -->
<script>
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add("revealed");
        }
      });
    },
    { threshold: 0.1, rootMargin: "0px 0px -50px 0px" },
  );

  document.querySelectorAll(".scroll-reveal").forEach((el) => observer.observe(el));
</script>
